__pycache__/
*.py[cod]

# Blackbox recordings and decoded data, and configuration backups. Both are
# produced next to whatever project the skill is used from.
logs/
backups/
*.bbl
*.csv
*.event

venv/
.venv/
.DS_Store

# Generated by `skillkit publish` for domain hosting. Its manifest lists only
# SKILL.md and omits scripts/, so committing it would ship an install path
# whose scripts are missing.
.well-known/
