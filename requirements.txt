pyserial>=3.5

# Optional. analyze_log.py uses numpy for its FFT when it is available and
# falls back to a pure-Python implementation when it is not, so the core
# install stays to a single dependency.
# numpy>=1.21
